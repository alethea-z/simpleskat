# Tests

- `skat.unit.test.mjs` covers rules, sorting, trick resolution, and point accounting.
- `browser.bdd.test.mjs` runs a real browser flow in headless mode through Playwright-in-Podman.
- `serve.mjs` provides the static test server used by the browser BDD.
